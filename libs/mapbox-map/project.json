{
  "name": "mapbox-map",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/mapbox-map/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "expo-module build"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "expo-module clean"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "expo-module lint"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "expo-module test"
      }
    },
    "prepare": {
      "executor": "nx:run-commands",
      "options": {
        "command": "expo-module prepare"
      }
    },
    "prepublishOnly": {
      "executor": "nx:run-commands",
      "options": {
        "command": "expo-module prepublishOnly"
      }
    }
  }
}
